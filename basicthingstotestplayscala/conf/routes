# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.FoodController.index

#Read all
GET     /foods                      controllers.FoodController.listFood

#Create one
POST    /foods                      controllers.FoodController.newFood

#Read one
GET     /food/:id                   controllers.FoodController.findById(id : Int)

#Delete one
DELETE  /food/:id                   controllers.FoodController.deleteFood(id : Int)

#Update one
PUT     /food/:id                   controllers.FoodController.updateFood(id : Int)




# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
